# Docker/Production Profile - Payment Service
# Optimized for EC2 deployment

spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        connection:
          provider_disables_autocommit: true
        query:
          plan_cache_max_size: 2048

logging:
  level:
    com.devision: INFO
    org.springframework: WARN
    org.hibernate: WARN
